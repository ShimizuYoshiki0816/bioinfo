# Docker インストール手順（Ubuntu / WSL2）

ここでは、Dockerをインストールするまでの手順と解説を行う。

---

## 1. パッケージリストの更新
まず、APTパッケージリスト*¹を更新する

```bash
sudo apt update
```

---

## 2. 必要パッケージのインストール
続いて、Dockerの公式リポジトリから安全にDockerエンジンをインストールするために、必要なパッケージをインストールする
```bash
sudo apt install -y apt-transport-https ca-certificates curl gnupg lsb-release
```

---

## 3. GPGキーの追加
さらに、配布元の正当性を確認するための電子署名キーを追加する
```bash
curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
  | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
```

---

## 4. Docker公式リポジトリの追加
APT経由でDockerの最新版をインストールできるようにするため、APTのリポジトリ一覧にDocker公式リポジトリを追加する
```bash
echo \
  "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] \
  https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" \
  | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```



---

## 5. 再度アップデート
APTリポジトリに変更内容を反映する

```bash
sudo apt update
```

---

## 6. Dockerのインストール

Dockerのコアシステムである
- docker-ce：Dockerエンジン本体  
- docker-ce-cli：CLI操作ツール  
- containerd.io：コンテナ実行ランタイム

のインストールを行う
```bash
sudo apt install -y docker-ce docker-ce-cli containerd.io
```

---

## 7. Docker Composeのインストール
複数コンテナを一括管理するツールである、Docker composeをインストールする

```bash
sudo curl -L \
  "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" \
  -o /usr/local/bin/docker-compose
```
ダウンロードしたdocker-composeの実行をするため、権限を付与する
```bash
sudo chmod +x /usr/local/bin/docker-compose
```

---

## 8. ユーザー権限の追加
ユーザー($USER)をDockerグループに追加しsudoなしでDockerを使えるようにする
```bash
sudo gpasswd -a $USER docker
```

---

## 9. Dockerの起動
DockerのバックグラウンドサービスであるDockerデーモンを起動する
```bash
sudo service docker start
```

---

## 10. WSL2の再起動

変更を確実に反映させるためにPCを再起動する。

---

## 補足

### *¹ APTとは
APT（Advanced Package Tool）は、UbuntuなどのDebian系Linuxで使用されるパッケージ管理ツール。  
ソフトウェアを取得・更新するためのリポジトリ情報を管理する。

---

### *² GPGキーとは
GPG Key（GNU Privacy Guard Key）は、ソフトウェア配布元の正当性を確認するための電子署名。
