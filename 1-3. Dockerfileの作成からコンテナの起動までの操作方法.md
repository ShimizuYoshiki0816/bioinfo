# Dockerの使い方
---
以下では簡単にLinuxの操作方法と、Dockerfileの作成、Docker imageの作成、並びにコンテナの起動方法について説明をする

## Dockerfileの作成
Dockerfileは、Docker imageを作成するための設定ファイルである。
### ディレクトリの作成

現在のディレクトリに新しいディレクトリを作成する

例えば

```
mkdir dir1
```

このコマンドを実行することでカレントディレクトリの下位にdir1というディレクトリが作成される

つづいてdir1に移動する

```
cd dir1
```
移動するとプロンプト（入力待ちの状態）が以下のようになる

```
username@:~/dir1$
```
表示が切り替わったことを確認したら次に進む

### ファイルの作成

ファイルの作成はtouchコマンドで行う

```
touch Dockerfile
```
ここではDockerfileという名前のファイルが作られる

Dockerfileという名前は慣習であり、任意のファイル名でも使用可能である。ただし、その場合は docker build 実行時に -f オプションでファイルを指定する必要がある
次にファイルの編集をvimコマンドで行う

```
vim Dockerfile
```

と入力をすると、エディタモードに切り替わる

画面が切り替わったら次に進む

### ファイルの編集

[Dockerfile](Dockerfile.md)の全文をコピーしてエディタモードで開いたDockerfileに張り付ける


貼り付け終わったらESCキーを押して、:wqと入力することで変更を保存しエディタモードを終了する

保存せずに終了する場合は、ESCキーを押した後:q!と入力することで終了する

Dockerfileについての解説はリンク先を参照
[Dockerfileの解説](https://github.com/ShimizuYoshiki0816/bioinfo/blob/main/1-4.%20Dockerfile%E8%A7%A3%E8%AA%AC.md)

## Docker image の作成
Dockerでは、Dockerfileをもとにイメージを作成することで、独自の環境を構築できる。

imageを作る方法は以下の通りである

```
docker build -t imagename .
```

このコマンドでは-tで作成するimageに名前を付けることができる

### 注意点
最後の .は前にスペースを入れる。こうすることでカレントディレクトリ内（現在のディレクトリ）にあるDockerfileを使ってimageを作成する。
もしカレントディレクトリ内に複数Dockerfileがある場合は -f を使って選択する必要がある。

## コンテナの実行
コンテナの実行は以下のコマンドで行う

```
docker run -it imagename
```
このコマンドで使われている-itはコンテナ内でもターミナルと同様にコマンドを打つことを可能にするオプションである。いわゆる対話モードである。

またコンテナにディレクトリをマウントすることも可能である。方法としては-vを使う。

例えば、

```
docker run -it -v $(pwd):/data imagename
```

とした場合、ここでは必要なデータの絶対パスを指定($(pwd))し、コンテナ側の/dataディレクトリに自身のディレクトリをマウントすることになる。

### 注意点

指定するパスは絶対パスでなければならないという決まりがある。また、上記の場合コンテナ内で作業する際に/dataに移る方法は

```
cd /data
```

とする必要がある


