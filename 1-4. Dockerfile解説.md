
# Dockerfileに記載されてあるコマンドの解説
---
## Dockerfileについて
本Dockerfileでは、以下の基本命令を用いて環境構築を行っている。
-  FROM：DockerイメージのベースとなるOSや既存環境を指定する命令

-  RUN：イメージのビルド時にコマンドを実行するための命令

-  ENV：環境変数を設定するための命令

# ベースイメージの指定
---
本Dockerfileでは、ベースイメージとしてGATKの公式が提供する最新のDockerイメージを使用している。
 ```
FROM broadinstitute/gatk:latest　
```
# ツールのインストール
---
ここではAPT経由でツールのインストールを行った。

```
RUN apt-get update && apt-get install -y \
    bwa \
    samtools \
    bcftools \
    vcftools \
    wget \
    unzip \
    openjdk-11-jdk \
    git \
    parallel \
    zlib1g-dev                
```
まずパッケージリストの更新を行い、その後、上記のbwa、samtools、bcftoolsなどのインストールを実行した。

#  Trimmomatic(0.39)のダウンロード及びセットアップ

---
以降APT経由ではインストールできないツールについて解説をする。

まず、Trimmomatic(0.39)のダウンロード及びセットアップは以下の手順で行った。
```
RUN wget -O Trimmomatic-0.39.zip http://www.usadellab.org/cms/uploads/supplementary/Trimmomatic/Trimmomatic-0.39.zip && \
    unzip Trimmomatic-0.39.zip && \
    mv Trimmomatic-0.39/trimmomatic-0.39.jar /usr/local/bin/trimmomatic.jar && \
    cp -r Trimmomatic-0.39/adapters /usr/local/share/trimmomatic && \
    rm -rf Trimmomatic-0.39 Trimmomatic-0.39.zip
```
各行では以下の操作を実行している

- 公式サイトからTrimomaticのダウンロード

- ファイルの解凍
- jarファイルのリネームと移動
- アダプターファイルを移動先にコピー
- 不要なファイルの削除
## 実行権限の付与
```
RUN printf '#!/bin/bash\njava -jar /usr/local/bin/trimmomatic.jar "$@"\n' > /usr/local/bin/trimmomatic && \
    chmod +x /usr/local/bin/trimmomatic
```
本来trimomaticは以下のコマンドを使用して実行する
```
java -jar /usr/local/bin/trimmomatic.jar ~~
```
上記のように実行権限を付けることで
```
trimomatic  ~~
```
というコマンドでTrimomaticを実行することができる
# PLINK（v1.9）のセットアップ
---

```
RUN wget -O plink.zip https://s3.amazonaws.com/plink1-assets/plink_linux_x86_64_20230116.zip && \
    unzip plink.zip && \
    mv plink /usr/local/bin/plink && \
    chmod +x /usr/local/bin/plink && \
    rm -rf plink.zip
```
続いてPLINKのセットアップもTrimomatic同様
- 公式からPlinkのダウンロード
- ファイルの解凍
- ファイルの移動
- 実行権限の付与
- 不要なファイルの削除
  という手順で行った。
# LinkImputeのセットアップ
---
Linkimputeは配布元のリンクが切れていたため、Githubからライブラリを導入処理を行った。
## commons-cli
```
RUN mkdir -p /opt/jarbuild \
 && wget -O /opt/jarbuild/commons-cli-1.4.jar \
    https://repo1.maven.org/maven2/commons-cli/commons-cli/1.4/commons-cli-1.4.jar
```
まず、/opt/jarbuild ディレクトリをimage内に作り、Javaのコマンドライン引数解析ライブラリ、であるcommons-cli-1.4.jarをそのディレクトリにダウンロードした。
## unpack commons-cli
```
RUN cd /opt/jarbuild \
 && jar xf commons-cli-1.4.jar
```
次に/opt/jarbuildに移動し、ファイルの解凍をした。
## build LinkImpute
```
RUN git clone https://github.com/danielmoney/LinkImpute.git /opt/LinkImpute \
 && cd /opt/LinkImpute/src \
 ```
続いて、GithubからLinkimputeのリポジトリを複製し、/opt/LinkImpute/srcに移動した。
 ## compile
 ```
 && mkdir -p build \
 && find . -name "*.java" > sources.txt \
 && javac -cp /opt/jarbuild/commons-cli-1.4.jar \
           -d build @sources.txt \
 ```
/opt/LinkImpute/srcの下位にbuildディレクトリを作成し、・~/src内の形式が.javaのファイルをリスト化し、外部ライブラリを参照しながら、それらをまとめてコンパイルして build に出力した。
 ## manifest
 ```
 && echo "Main-Class: Executable.LinkImpute" > manifest.txt \
 ```
メインクラスを指定した
 ## fat jar (LinkImpute + commons-cli)
 ```
 && jar cvfm /usr/local/bin/LinkImpute.jar manifest.txt \
      -C build . \
      -C /opt/jarbuild org \
  && rm -rf /opt/LinkImpute /opt/jarbuild
```
依存関係を内包したfat JARを生成し、不要なファイルの削除をした。
# Python ライブラリのインストール
---
```
RUN pip3 install --upgrade pip && \
    pip3 install pandas numpy scikit-learn matplotlib keras tensorflow
```
Pythonのパッケージ管理ツールを更新した上で、データ解析および機械学習に必要な主要ライブラリ群を導入した。
# RとRパッケージのインストール
---
```
RUN apt-get update && apt-get install -y r-base && \
    Rscript -e "install.packages(c('rrBLUP', 'devtools'), repos='https://cloud.r-project.org')" && \
    Rscript -e "install.packages('sommer', repos='https://cloud.r-project.org')" && \
    Rscript -e "install.packages('BGLR', repos='https://cloud.r-project.org')"
```
R環境を構築した上で、ゲノム予測および遺伝解析に用いられる主要パッケージを導入した。
# 言語設定
---
```
RUN apt-get update && apt-get install -y locales && \
    sed -i -e 's/# \(ja_JP.UTF-8\)/\1/' /etc/locale.gen && \
    locale-gen

ENV LANG=ja_JP.UTF-8
ENV LANGUAGE=ja_JP:ja
ENV LC_ALL=ja_JP.UTF-8
```
コンテナ内の言語設定を日本語にした
