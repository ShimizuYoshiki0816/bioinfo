# WSL2（Ubuntu）セットアップ手順

## 1. Windows機能の有効化
---
まず、Windows側でLinuxの起動に必要な機能の有効化を行う。手順は以下の通りである。

検索バーに「Windowsの機能の有効化または無効化」と入力する

検索結果を左クリックする

開いたウィンドウ上にある

- Linux 用 Windows サブシステム
- 仮想マシン プラットフォーム

のチェックボックスをオンにする

変更を行った際は、画面上の指示に従いコンピューターを再起動する

## 2. 管理者権限でコマンドを実行
---
続いて、OSの設定変更を行うために、PowerShellまたはコマンドプロンプトを管理者モードで起動する。

起動方法は以下の通りである
- Windowsキーを押す  
- 「cmd」または「PowerShell」と入力  
- 表示された「管理者として実行」  をクリックする

## 3. WSL2のインストール
---
WindowsでLinux OSを使用するにはLinuxカーネルというOSの核が必要になる

LinuxカーネルはLinuxディストリビューションというパッケージに含まれている

以下は、ディストリビューションをインストールするコマンドである

```bash
wsl --install
```
このコマンドにより、Ubuntuの最新版のディストリビューションをインストールすることが可能である

インストールされたディストリビューションの確認は
```
wsl -l -v
```
このコマンドで以下の出力がされる
```
  NAME      STATE           VERSION
* Ubuntu    Stopped         2
```

出力内容は左から、インストールされているディストリビューションの名前、状態、WSLのバージョンをそれぞれ示している。また、Ubuntuの前についている、*は既定のディストリビューションであることを示している

## 4. Ubuntuの初期設定
---
Ubuntuの初回起動時に
- ユーザー名の設定
- パスワード

  の設定を行う

### ユーザー名の設定  
```bash
Enter new UNIX username: yourname
```

使用可能文字
- 英小文字(a-z)
- 数字(0-9)
- ハイフン(-)
- アンダースコア(_)

### パスワードの設定  

```bash
New password:your_pass
Retype new password:your_pass
```

このパスワードは sudo 実行時に使用する。

### 注意点

パスワード入力時に画面に入力した文字は表示されない

## 5. パッケージの更新
---
```bash
sudo apt update
sudo apt full-upgrade
```

- apt update：パッケージリストの更新
- apt full-upgrade：システム全体の更新

### 以上でWSL2のセットアップは完了

# ゲノムデータの取得
---
本研究では、[Linら<sup>10)</sup>](https://pubmed.ncbi.nlm.nih.gov/33203997/)の研究で使用されたトラフグ（_Takifugu rubripes_）のアンプリコンシーケンスのデータを使用している。これらはDDBJにアーカイブされており二次利用が可能となっている。

WSL上で下記のコマンドを実行することで入手可能
```
seq 0 239 | xargs -n 1 -P 8 -I {} bash -c '
  drx=$(printf "DRX%06d" $((223812 + {})))
  drr=$(printf "DRR%06d" $((233598 + {})))
  wget https://ddbj.nig.ac.jp/public/ddbj_database/dra/fastq/DRA010/DRA010341/${drx}/${drr}_1.fastq.bz2
'
```
```
seq 0 239 | xargs -n 1 -P 8 -I {} bash -c '
  drx=$(printf "DRX%06d" $((223812 + {})))
  drr=$(printf "DRR%06d" $((233598 + {})))
  wget https://ddbj.nig.ac.jp/public/ddbj_database/dra/fastq/DRA010/DRA010341/${drx}/${drr}_2.fastq.bz2
'
```

続いてリファレンスゲノムのダウンロードは下記のコマンドを実行する
```
wget https://ftp.ncbi.nlm.nih.gov/genomes/all/GCF/000/180/615/GCF_000180615.1_FUGU5/GCF_000180615.1_FUGU5_genomic.fna.gz
```

